# Cloudflare Pages 安全標頭(2026-07-15;見 CHANGELOG v2.44.3)
# clickjacking:me/settings 有即時寫入(公開開關/改名),絕不可被外站 iframe 疊層誘點。
#
# 🚨 未解問題(2026-08-12 實測,v2.61.0):**下面這些具體路徑的規則沒有生效**。
#    正式站與 pages.dev 原始站的 /me.html /settings.html /me-input.html 都只拿到
#    最後那條 /* 的 nosniff+Referrer-Policy,X-Frame-Options 與 CSP 完全不出現。
#    已排除的可能:
#      · 不是 CDN 那層拿掉的 —— pages.dev 原始站同樣沒有
#      · 不是設定檔沒被消化 —— /_headers 與 /_redirects 都回 404(已被 Pages 吃掉),
#        且 _redirects 的 /en/ → 301 在 pages.dev 確實有效
#      · 不是規則順序 —— 把 /* 移到最前面實測仍然沒有 XFO(已還原成本來的寫法)
#    影響:主站的 me/settings 目前可被外站 iframe 疊層(my. 子網域不受影響,
#    那邊每個回應都是 Worker 自組、標頭由 my-worker.js 的 secHeaders() 補上)。
#    下一步待查:Pages 專案設定是否有覆蓋、或改用 Worker/Transform Rule 供應標頭。

/me.html
  X-Frame-Options: DENY
  Content-Security-Policy: frame-ancestors 'none'
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer

/settings.html
  X-Frame-Options: DENY
  Content-Security-Policy: frame-ancestors 'none'
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer

# me-input 需被 me.html 同源 iframe 嵌入 → SAMEORIGIN(不可 DENY)
/me-input.html
  X-Frame-Options: SAMEORIGIN
  Content-Security-Policy: frame-ancestors 'self'
  X-Content-Type-Options: nosniff

# 全站基本防線
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer
